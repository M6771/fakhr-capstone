/** Centers & Professionals — aligned with Library / Care Path mocks */
export const dirColors = {
  background: "#F5F6FA",
  primary: "#7C82A1",
  textPrimary: "#1F2937",
  textSecondary: "#6B7280",
  card: "#FFFFFF",
  chip: "#E5E7EB",
  searchBg: "#E5E7EB",
  openBg: "#E8F5E9",
  openText: "#22C55E",
  busyBg: "#F3F4F6",
  busyText: "#6B7280",
  star: "#FBBF24",
  ratingBadgeBg: "rgba(255,255,255,0.95)",
  phoneBorder: "#D1D5DB",
} as const;

export const dirRadii = {
  card: 22,
  pill: 999,
  sm: 10,
  md: 12,
} as const;

export const dirSpacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  screen: 20,
} as const;

export const dirShadow = {
  shadowColor: "#000",
  shadowOffset: { width: 0, height: 4 },
  shadowOpacity: 0.07,
  shadowRadius: 10,
  elevation: 4,
};

export const dirShadowSoft = {
  shadowColor: "#000",
  shadowOffset: { width: 0, height: 2 },
  shadowOpacity: 0.05,
  shadowRadius: 8,
  elevation: 3,
};
